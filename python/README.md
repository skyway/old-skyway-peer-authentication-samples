# Python Authentication Sample Server

## Set up

no setup is required

## Run the server

Note this sample is written for Python2.

```bash
$ python sample.php 
```

You can now post to `http://localhost:8080/authenticate` to generate a credential.